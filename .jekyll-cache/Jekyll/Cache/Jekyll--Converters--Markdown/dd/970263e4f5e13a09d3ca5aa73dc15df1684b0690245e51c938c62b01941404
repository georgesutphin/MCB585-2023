I"›<p>To participate in both in-class exercises and homework, you will need access to a computer with the current versions of the following installed:</p>

<ol>
  <li>the <a href="https://cran.r-project.org/mirrors.html">R software</a> itself, and</li>
  <li><a href="https://www.rstudio.com/products/rstudio/download/#download">RStudio Desktop</a>.</li>
</ol>

<p>You also need to download some files to follow this lesson:</p>

<ol>
  <li>Make a new folder in your Desktop called <code class="language-plaintext highlighter-rouge">MCB585</code>.</li>
  <li>Download <a href="./data/MCB585-sample-data.zip">MCB585-sample-data.zip</a>
and move the file to this folder.</li>
  <li>If itâ€™s not unzipped yet, double-click on it to unzip it. You should end up
with a new folder called <code class="language-plaintext highlighter-rouge">data</code>.</li>
  <li>You can access this folder through your systems file explorer, or by using command-prompt navigation with:</li>
</ol>

<p><strong>Unix shell:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">ls</span>
<span class="nv">$ </span><span class="nb">cd </span>Desktop/MCB585/data
</code></pre></div></div>

<p><strong>Windows Command Prompt:</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> <span class="nb">dir</span>
<span class="o">&gt;</span> <span class="nb">cd </span>c:<span class="se">\U</span>sers<span class="se">\&lt;</span>username&gt;<span class="se">\D</span>esktop<span class="se">\M</span>CB585<span class="se">\d</span>ata
</code></pre></div></div>

<p><strong>MacOS</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cd</span> /Users/&lt;username&gt;/Desktop
</code></pre></div></div>
:ET